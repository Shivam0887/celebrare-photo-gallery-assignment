const Navbar = () => {
  return (
    <header className="sticky top-0 z-50 py-4 bg-background px-10 border-b flex justify-between">
      <nav>
        <a href="/">
          <h1 className="text-2xl font-bold">Gallerize</h1>
        </a>
      </nav>
    </header>
  );
};

export default Navbar;
